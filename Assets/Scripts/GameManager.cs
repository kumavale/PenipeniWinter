using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //field = new int[FIELD_HEIGHT, FIELD_WIDTH];

        //}
        //GameObject pen = (GameObject)Instantiate(peni[0], new Vector3(0.0f, 0.0f, 0.0f), Quaternion.identity);
        //pen.transform.parent = canvas.transform;
        //pen.transform.position = new Vector3(0.0f, 0.0f, 0.0f);
    }

    // Update is called once per frame
    void Update()
    {
        //Debug.Log("aaa");
    }
}
